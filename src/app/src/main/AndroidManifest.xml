<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.delock.delockApplication">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <uses-feature
        android:name="android.hardware.nfc"
        android:required="true" />

    <application
        android:icon="@drawable/grey_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/grey_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <service android:name=".IPFSDaemonService" android:exported="false"/>
        <activity
            android:name="com.app.delock.delockApplication.browse.BrowseActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.app.delock.delockApplication.item.ItemActivity"
            android:label="@string/title_activity_Item" />
        <activity
            android:name="com.app.delock.delockApplication.settings.SettingsActivity"
            android:label="@string/title_activity_settings" />
        <!-- <activity -->
        <!-- android:name=".browse.BrowseActivity" -->
        <!-- android:label="Browse" /> -->
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="com.app.delock.delockApplication.maps.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name="com.app.delock.delockApplication.dashboard.DashboardActivity"
            android:label="@string/title_activity_dashboard"
            android:theme="@style/AppTheme" />
        <activity
            android:name="com.app.delock.delockApplication.my_profile.MyProfileActivity"
            android:label="@string/title_activity_my_profile"
            android:theme="@style/AppTheme" />
        <activity android:name="com.app.delock.delockApplication.check_availability.AvailabilityActvity" />
        <activity
            android:name="com.app.delock.delockApplication.unlock_item.UnlockActivity"
            android:label="@string/title_activity_unlock"
            android:theme="@style/AppTheme" />
        <activity android:name="com.app.delock.delockApplication.nfc.NFCSpeakerActivity" />
        <activity android:name="com.app.delock.delockApplication.nfc.NFCReaderSpeakerActivity" />
    </application>

</manifest>